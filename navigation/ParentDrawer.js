import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ParentDrawer = () => {
  return (
    <View>
      <Text>ParentDrawer</Text>
    </View>
  );
};

export default ParentDrawer;

const styles = StyleSheet.create({});
